#include <stdio.h>
#include <string.h>



int main(){
    char c[10];
    printf("Enter the string:");
    fgets(c,10,stdin);
    int len = strlen(c);
    char sub[5];
    printf("enter the substring:");
    fgets(sub,5,stdin);
    int len1 = strlen(sub);
    

   

}