#include <stdio.h>
#include<stdlib.h>
#include "LinkedList.h"

int main(){
   push_front(30);
   push_front(20);
   push_front(10);
   push_back(40);
   push_back(50);
   insert_position(25,3);
   delete_position(2);
   display();
    
    
}